<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Gemini AI Chatbot</title>

<style>
body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: #0f172a;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.chat-container {
    width: 360px;
    background: #111827;
    padding: 15px;
    border-radius: 12px;
    color: white;
    box-shadow: 0 0 15px rgba(0,0,0,0.5);
}

h2 {
    text-align: center;
    margin-bottom: 10px;
}

#chat-box {
    height: 320px;
    overflow-y: auto;
    background: #020617;
    padding: 10px;
    border-radius: 8px;
}

.message {
    margin: 8px 0;
    line-height: 1.4;
}

.user {
    text-align: right;
    color: #38bdf8;
}

.bot {
    text-align: left;
    color: #a7f3d0;
}

.input-area {
    display: flex;
    margin-top: 10px;
}

input {
    flex: 1;
    padding: 10px;
    border-radius: 6px;
    border: none;
    outline: none;
}

button {
    margin-left: 6px;
    padding: 10px 14px;
    border: none;
    border-radius: 6px;
    background: #22c55e;
    cursor: pointer;
    font-weight: bold;
}
</style>
</head>

<body>

<div class="chat-container">
    <h2>🤖 Gemini Chatbot</h2>
    <div id="chat-box"></div>

    <div class="input-area">
        <input id="user-input" placeholder="Type your message..." />
        <button onclick="sendMessage()">Send</button>
    </div>
</div>

<script>
const API_KEY = AIzaSyC_YFszlUAsC_SP4rRafq4BhJ7EuvqsH9w;
const chatBox = document.getElementById("chat-box");

function addMessage(text, sender) {
    const div = document.createElement("div");
    div.className = "message " + sender;
    div.innerText = text;
    chatBox.appendChild(div);
    chatBox.scrollTop = chatBox.scrollHeight;
}

async function sendMessage() {
    const input = document.getElementById("user-input");
    const userText = input.value.trim();
    if (!userText) return;

    addMessage(userText, "user");
    input.value = "";

    try {
        const response = await fetch(
            "https://generativelanguage.googleapis.com/v1/models/gemini-pro:generateContent?key=" + AIzaSyC_YFszlUAsC_SP4rRafq4BhJ7EuvqsH9w,
            {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                    contents: [{
                        parts: [{ text: userText }]
                    }]
                })
            }
        );

        const data = await response.json();
        const reply =
            data.candidates?.[0]?.content?.parts?.[0]?.text ||
            "No response from Gemini.";

        addMessage(reply, "bot");

    } catch (err) {
        addMessage("❌ Error connecting to Gemini API", "bot");
        console.error(err);
    }
}
</script>

</body>
</html>
